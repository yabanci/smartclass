// Smoke test — verifies the app widget renders without crashing.
import 'package:flutter_test/flutter_test.dart';

void main() {
  // The original counter test is superseded by the auth/device tests.
  // This file is kept as a placeholder to avoid stale test files.
  test('placeholder', () => expect(true, isTrue));
}
